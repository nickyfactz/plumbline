# Routing expectations

Each prompt selects one path only. Direct work creates no Plumbline artifacts. One-off explicit use never creates a router. Automatic routing is available only when the initialized fixture contains `.agents/skills/plumbline-router/`. Review is report-only. Closeout requires acceptance before deleting transient artifacts.
