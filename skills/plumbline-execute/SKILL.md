---
name: plumbline-execute
description: Execute an active Plumbline feature plan through bounded checkpoints with evidence before advancement.
---

# Execute

This is the explicit user-facing wrapper. Run the `plumbline-execute-engine` skill now. Keep the main thread as the plan and Git authority.
