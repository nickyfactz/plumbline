interface:
  display_name: "Plumbline Execute"
  short_description: "Advance a feature through verified checkpoints"
  default_prompt: "Execute the current Plumbline checkpoint plan."
policy:
  allow_implicit_invocation: false
