interface:
  display_name: "Plumbline Execute"
  short_description: "Complete the active plan through verified checkpoints"
  default_prompt: "Execute the active Plumbline plan through all remaining checkpoints."
policy:
  allow_implicit_invocation: false
