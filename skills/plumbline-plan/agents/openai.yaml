interface:
  display_name: "Plumbline Plan"
  short_description: "Build one live checkpoint plan for a feature"
  default_prompt: "Create the live checkpoint plan for this feature."
policy:
  allow_implicit_invocation: false
