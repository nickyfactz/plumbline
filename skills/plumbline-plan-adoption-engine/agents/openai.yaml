interface:
  display_name: "Plumbline Plan Adoption Engine"
  short_description: "Internal Relay plan-normalization workflow"
  default_prompt: "Normalize the selected sufficient source into the smallest Relay execution contract."
policy:
  allow_implicit_invocation: true
