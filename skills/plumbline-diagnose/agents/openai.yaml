interface:
  display_name: "Plumbline Diagnose"
  short_description: "Find the minimum sufficient root cause"
  default_prompt: "Trace the root cause of this bug or regression before correcting it."
policy:
  allow_implicit_invocation: false
